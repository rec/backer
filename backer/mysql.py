# Stub


def run(execute, name, target=None, source=None,
        every='day@4:32',
        type='mysql',
        tables=None,
        user='user',
        password='password'):
    execute.run('something')
